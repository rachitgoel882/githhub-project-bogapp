server:
  port: 8855
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blogapp
    username: root
    password: Himanshu@882
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  application:
    name: blogapp
